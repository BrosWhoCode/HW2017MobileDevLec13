<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="buttonStyle">@style/DEIButtonStyle</item>
    </style>

    <style name="DEITextStyle">
        <item name="android:textSize">22dp</item>
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textStyle">bold|italic</item>
        <item name="android:padding">5dp</item>
    </style>

    <style name="DEISofterTextStyle" parent="DEITextStyle">
        <item name="android:textStyle">italic</item>
        <item name="android:scaleX">.75</item>
    </style>

    <style name="DEISofterBorderedTextStyle" parent="DEISofterTextStyle">
        <item name="android:background">@drawable/border_rounded_shape</item>
    </style>

    <style name="DEIButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/button_background</item>
    </style>

</resources>
